# WorldCup2k26

## Auto-sync live results

This project can automatically update `matches.json` from football-data.org live results using the GitHub Actions workflow in `.github/workflows/update-matches.yml`.

The workflow requires a repository secret named `FOOTBALL_DATA_API_KEY`.
